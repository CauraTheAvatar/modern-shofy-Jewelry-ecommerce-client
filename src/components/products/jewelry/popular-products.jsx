import React from 'react';

const PopularProducts = () => {
  React.useEffect(() => {
    // Cleanup if anything was needed
    return () => {
      // Cleanup function
    };
  }, []);

  return null;
};

export default PopularProducts;